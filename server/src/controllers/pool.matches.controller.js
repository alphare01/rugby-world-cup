//-----------REQUIRE-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

const {getAllPoolMatches} = require('../models/pool.matches.model');

//-----------FUNCTION----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

async function httpGetAllPoolMatches(req, res) {
    return res.status(200).json(await getAllPoolMatches());
};

//-----------EXPORT------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

module.exports = {
    httpGetAllPoolMatches,
};